.PHONY: build test clean fmt

# Builds the optimized .wasm using the Stellar CLI.
# If your setup still uses the older CLI name, replace with: soroban contract build
build:
	stellar contract build

test:
	cargo test

clean:
	cargo clean

fmt:
	cargo fmt
